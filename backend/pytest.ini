[pytest]
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: marks tests that make real network calls to Gemini/Supabase (deselected by default; run explicitly with `pytest -m live`)
addopts = -m "not live"